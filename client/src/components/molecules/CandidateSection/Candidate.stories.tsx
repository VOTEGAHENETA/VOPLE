import { Meta, Story } from '@storybook/react';
import CandidateSection from '.';

export default {
  title: 'Components/CandidateSection',
  component: CandidateSection,
} as Meta;

export const Primary: Story = () => <CandidateSection />;

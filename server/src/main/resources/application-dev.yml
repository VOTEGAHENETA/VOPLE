spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:tcp://localhost/~/server
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      #      ddl-auto: update
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
        id:
          new_generator_mappings: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
#      mode: never

  data:
    redis:
      host: localhost
      port: 6379
    rtmp:
      host: rtmp://i12b102.p.ssafy.io:1935/live/
    hls:
      host-prefix: http://i12b102.p.ssafy.io:8050/hls/
      host-postfix: .m3u8

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: c26c8ad4cf28c0e815b4024f01ca026c
            client-secret: HofU5oCSTB9koEYkZ66HFT8nXTFEBj0N
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
            client-name: kakao

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

paging:
  default-page-size: 10
  default-page-number: 0

logging.level:
  org.hibernate.SQL: DEBUG
  org.springframework:
    security: DEBUG
    web.socket: DEBUG
    messaging: DEBUG

springdoc:
  swagger-ui:
    path: /index.html
  api-docs:
    path: /v3/api-docs


